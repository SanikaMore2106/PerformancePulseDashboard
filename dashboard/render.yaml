services:
  - type: web
    name: performance-pulse-frontend
    env: python
    rootDir: frontend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run dashboard_app.py --server.port $PORT --server.address 0.0.0.0"